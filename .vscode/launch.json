{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Lancer le convertisseur",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/convertisseur.py",
            "python": "${workspaceFolder}/.venv/bin/python",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
